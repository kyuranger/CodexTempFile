--- a/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_carriers_motion.css
+++ b/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_carriers_motion.css
@@ -1,8 +1,11 @@
-/* CARRIER FIELD REVEAL: the accepted scene and route copy remain unchanged. */
-.carriers__media { overflow: hidden; }
+/* P01: a later reveal inside the page's established desktop media radius. */
+.carriers__media { --carrier-radius: 0px; overflow: hidden; border-radius: var(--carrier-radius); }
+@media (min-width: 961px), (min-width: 701px) and (orientation: landscape) {
+  .carriers__media { --carrier-radius: var(--story-radius); }
+}
 @media (prefers-reduced-motion: no-preference) {
   .carriers[data-carrier-stage] .carriers__media {
-    clip-path: inset(calc((1 - var(--carrier-progress)) * 6%) calc((1 - var(--carrier-progress)) * 16%) calc((1 - var(--carrier-progress)) * 4%) calc((1 - var(--carrier-progress)) * 4%));
+    clip-path: inset(calc((1 - var(--carrier-progress)) * 6%) calc((1 - var(--carrier-progress)) * 16%) calc((1 - var(--carrier-progress)) * 4%) calc((1 - var(--carrier-progress)) * 4%) round var(--carrier-radius));
   }
   .carriers[data-carrier-stage] .carriers__media img {
     transform: scale(calc(1 + (1 - var(--carrier-progress)) * 0.035));
--- a/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_reading.css
+++ b/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_reading.css
@@ -1,4 +1,4 @@
-/* A8-C: physical ribbon media hands attention to one semantic reading field. */
+/* A8-C P01: one identifier-to-information story, with a visible handoff. */
 @font-face {
   font-family: "Tusky Reading";
   font-style: normal;
@@ -24,18 +24,18 @@
 .reading__composition { display: grid; grid-template-columns: minmax(0, 1.45fr) minmax(0, 1fr); gap: clamp(36px, 4.4vw, 64px); align-items: start; margin-top: 64px; }
 .reading__media { margin: 36px 0 0; min-width: 0; }
 .reading__scene { position: relative; overflow: hidden; }
+.reading__image { position: relative; transform-origin: 35% 60%; }
 .reading__scene img { display: block; width: 100%; height: auto; }
 /* Static screen matte removes baked-in AI status and verification claims.
    This mask never reveals or animates into a result. */
 .reading__screen-mask { position: absolute; left: 74.75%; top: 57.1%; width: 19.65%; height: 15.5%; background: #f7f7f5; transform: skewY(-1deg); border-radius: 1.2% 1.2% 5% 5%; }
 .reading__media figcaption { margin-top: 14px; color: var(--type-secondary); font-size: 12px; line-height: 1.7; }
 .reading__field { min-width: 0; padding-right: clamp(8px, 2vw, 28px); }
-.reading__example { margin: 0 0 28px; display: flex; flex-wrap: wrap; gap: 8px 18px; color: var(--type-identity); font-size: 13px; line-height: 1.6; }
-.reading__example span { color: var(--type-secondary); font-size: 12px; align-self: center; }
+.reading__example { margin: 0 0 28px; color: var(--type-identity); font-size: 14px; line-height: 1.6; }
 .reading dl { margin: 0; }
 .reading dt { font-size: 13px; line-height: 1.65; font-weight: 400; color: var(--type-detail); }
 .reading dd { margin: 10px 0 0; font-weight: 350; line-height: 1.3; }
-.reading__product dd { font-size: clamp(36px, 3.4vw, 49px); }
+.reading__product dd { font-size: clamp(40px, 3.75vw, 54px); }
 .reading__code { margin-top: 24px; }
 .reading__code dd { font-size: clamp(20px, 1.65vw, 24px); overflow-wrap: anywhere; }
 .reading .reading__facts { margin-top: 32px; padding-top: 26px; border-top: 1px solid #daddd7; display: flex; flex-wrap: wrap; gap: 24px 32px; }
@@ -83,10 +83,16 @@
   .reading__scope { margin-top: 38px; padding-inline: 8px; }
 }
 @media (prefers-reduced-motion: no-preference) {
-  .reading[data-handoff] .reading__field { transform: translateY(calc((1 - var(--reading-progress)) * 12px)); color: color-mix(in srgb, var(--type-section) calc(var(--reading-progress) * 100%), var(--type-secondary)); }
-  .reading[data-handoff] .reading__media { transform: translateX(calc((1 - var(--reading-progress)) * 6px)); }
+  .reading[data-handoff] .reading__field {
+    transform-origin: left top;
+    transform: translate(calc((var(--reading-progress) - 1) * 28px), calc((1 - var(--reading-progress)) * 44px)) scale(calc(0.94 + var(--reading-progress) * 0.06));
+    color: color-mix(in srgb, var(--type-section) calc(var(--reading-progress) * 100%), var(--type-secondary));
+  }
+  .reading[data-handoff] .reading__image {
+    transform: translateX(calc((var(--reading-progress) - 1) * 1%)) scale(calc(1.055 - var(--reading-progress) * 0.055));
+  }
 }
 @media (max-width: 960px) and (prefers-reduced-motion: no-preference) {
-  .reading[data-handoff] .reading__media { transform: none; }
-  .reading[data-handoff] .reading__field { transform: translateY(calc((1 - var(--reading-progress)) * 8px)); }
+  .reading[data-handoff] .reading__image { transform: none; }
+  .reading[data-handoff] .reading__field { transform: translateY(calc((1 - var(--reading-progress)) * 20px)); }
 }
--- a/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_reading.js
+++ b/prototypes/phase-2d-a5-3/tusky_phoenix/assets/tusky_phoenix_reading.js
@@ -1,4 +1,4 @@
-// A8-C: C3 framing and C4 reading attention. No query, value changes or scroll rail.
+// A8-C P01: separate perceptual windows for C3 reveal and C4 reading handoff.
 const motion = window.matchMedia('(prefers-reduced-motion: no-preference)');
 const stories = [
   { root: document.querySelector('.carriers'), anchor: '.carriers__media', property: '--carrier-progress', attribute: 'data-carrier-stage' },
@@ -16,11 +16,25 @@
   const samples = stories.map(story => {
     const anchor = story.root.querySelector(story.anchor);
     let top = anchor.getBoundingClientRect().top;
-    if (compact && story.attribute === 'data-handoff') {
-      const field = story.root.querySelector('.reading__field');
-      top += field.offsetTop - anchor.offsetTop;
+    let distance;
+    let travelled;
+    if (story.attribute === 'data-carrier-stage') {
+      const sectionTop = story.root.getBoundingClientRect().top;
+      // Both the chapter and its media must be visibly entered before advancing.
+      travelled = Math.min(height * 0.2 - sectionTop, height * 0.36 - top);
+      distance = height * 0.28;
+    } else {
+      if (compact) {
+        const field = story.root.querySelector('.reading__field');
+        top += field.offsetTop - anchor.offsetTop;
+      }
+      travelled = height * (compact ? 0.72 : 0.4) - top;
+      distance = height * (compact ? 0.22 : 0.28);
+      // Finish within the existing chapter, including a short desktop at page end.
+      const room = story.root.getBoundingClientRect().bottom - top - height * (compact ? 0.28 : 0.6);
+      distance = Math.min(distance, Math.max(1, room));
     }
-    return { story, progress: Math.max(0, Math.min(1, (height * 0.86 - top) / (height * (compact ? 0.4 : 0.46)))) };
+    return { story, progress: Math.max(0, Math.min(1, travelled / distance)) };
   });
   for (const { story, progress } of samples) {
     const state = story.attribute === 'data-carrier-stage'
--- a/prototypes/phase-2d-a5-3/tusky_phoenix/index.html
+++ b/prototypes/phase-2d-a5-3/tusky_phoenix/index.html
@@ -594,7 +594,7 @@
         </div>
       </div>
     </section>
-    <section class="carriers" id="alternative-carriers" aria-labelledby="carriers-heading" data-phase="phase-2d-a8-c" data-story="One Object, Alternative Carriers" data-copy-status="PROTOTYPE_COPY_NOT_APPROVED" data-media-status="AI_GENERATED_REALISTIC_CONCEPT_MEDIA / NOT_ALLOWED_AS_EVIDENCE" data-current-capability-status="NEEDS_VERIFICATION">
+    <section class="carriers" id="alternative-carriers" aria-labelledby="carriers-heading" data-phase="phase-2d-a8-c-p01" data-story="One Object, Alternative Carriers" data-copy-status="PROTOTYPE_COPY_NOT_APPROVED" data-media-status="AI_GENERATED_REALISTIC_CONCEPT_MEDIA / NOT_ALLOWED_AS_EVIDENCE" data-current-capability-status="NEEDS_VERIFICATION">
       <header class="carriers__intro">
         <p class="carriers__eyebrow">标识与载体</p>
         <h2 id="carriers-heading"><span>标识</span><span>落在合适的载体上</span></h2>
@@ -631,22 +631,24 @@
         <p class="carriers__scope">具体赋码、读取与系统接入方式，需结合实际应用确认。</p>
       </div>
     </section>
-    <section class="reading" id="product-information" aria-labelledby="reading-heading" data-phase="phase-2d-a8-c" data-copy-status="PROTOTYPE_COPY_NOT_APPROVED" data-example-source="HISTORICAL_STATIC_SAMPLE" data-current-capability-status="NEEDS_VERIFICATION">
+    <section class="reading" id="product-information" aria-labelledby="reading-heading" data-phase="phase-2d-a8-c-p01" data-copy-status="PROTOTYPE_COPY_NOT_APPROVED" data-example-source="HISTORICAL_STATIC_SAMPLE" data-current-capability-status="NEEDS_VERIFICATION">
       <header class="reading__intro">
         <p class="reading__eyebrow">产品查询</p>
         <h2 id="reading-heading"><span>从标识</span><span>读到产品信息</span></h2>
-        <p class="reading__lead">把产品标识作为阅读入口，了解产品名称、生产日期与批号等具体信息。</p>
+        <p class="reading__lead">以产品标识为入口，读取名称、生产日期、批号与规格。</p>
       </header>
       <div class="reading__composition">
       <figure class="reading__media" data-media-status="AI_GENERATED_REALISTIC_CONCEPT_MEDIA / NOT_ALLOWED_AS_EVIDENCE">
         <div class="reading__scene">
+          <div class="reading__image">
           <img src="assets/seal_ribbon_traceability_scene.webp" width="1448" height="1086" alt="包装碳带上的条码与二维码、未贴包装标签的裸卷，以及作为阅读场景陪衬的手机。" loading="lazy" decoding="async">
           <span class="reading__screen-mask" aria-hidden="true"></span>
+          </div>
         </div>
-        <figcaption>碳带应用示意</figcaption>
+        <figcaption>产品上的标识</figcaption>
       </figure>
       <div class="reading__field" aria-labelledby="reading-example">
-        <p class="reading__example" id="reading-example">产品信息示例<span>历史展示数据</span></p>
+        <p class="reading__example" id="reading-example">产品信息示例</p>
         <dl class="reading__identity">
           <div class="reading__product">
             <dt>产品名称</dt>
@@ -673,7 +675,7 @@
         </dl>
       </div>
       </div>
-      <p class="reading__scope">配图与历史示例并非同一产品记录。信息内容与读取方式需按具体应用确认。<span>信息可查不等同于真伪判定。</span></p>
+      <p class="reading__scope">信息内容为独立示例，具体字段与读取方式需按应用确认。<span>信息可查不等同于真伪判定。</span></p>
     </section>
   </main>
 </body>
