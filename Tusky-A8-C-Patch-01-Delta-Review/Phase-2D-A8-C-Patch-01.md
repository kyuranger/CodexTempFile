# Phase 2D-A8-C Patch 01

状态：PHASE_2D_A8_C_P01_REVIEW_READY。此状态表示补丁已可交付评审，仍需 Product Owner 接受视觉与文案。

## 当前决策与范围

A8-C 已被 Product Owner 判定 NOT ACCEPTED。原 `Phase-2D-A8-C-Decision.md` 及旧 ZIP 中的 REVIEW_READY 仅为历史提交状态，不代表接受。本补丁替代 A8-C 的 C3 时机、圆角及 C4 文案／交接动效判断；A7 与 A8 已接受的前章方向继续有效。

本次仅修复 C3/C4。Hero 与 C2 的 HTML/CSS/JS 保持原样；批准的载体与碳带媒体不重新生成、不改像素。没有 C5、动画框架、服务端查询、认证流程或发布。

## 实际改动

- C3 同时检查章节与主图的进入程度，才开始载体展开。开始约在章节顶部到达视口 20%、主图顶部到达视口 36% 时，取较晚者；进度用 28% 视口高度完成。没有新增 sticky 或滚动轨道。
- C3 在桌面与平板横屏使用既有 `--story-radius`：1440 为 26px，1024 / 820 横屏为 22px。容器 overflow clipping 与动态 clip-path 使用同一圆角，图片比例保持不变。
- C4 聚焦“产品标识作为入口，可以读取具体信息”。删除“历史展示数据”这个可见副标签，仅保留“产品信息示例”；历史样本来源仍作为内部 provenance。
- C4 桌面由实物媒体的轻微构图变化，交接到逐渐变大、回正并增强对比的信息区。图片和固定手机遮罩在同一变换层，遮罩不随进度揭开。
- C4 手机／平板以信息区进入视野为起点，仅保留 20px 纵向回位及对比变化，媒体静止；六项数据始终存在且不变。
- 1024 宽度下章节接近页尾，原标准范围只能走到约 49%。补丁将范围限制在现有章节的自然剩余距离内；真实浏览器页尾到达 0.9969，状态 information（不足 1px 的滚动取整）。不增加留白。

C4 标题仍是单一语义 H2；产品名称作为主值、日期和批号作为次层、字段标签和范围说明作为辅助层。字体子集随新文案更新，继续使用真实可变字重。

## 验证与边界

- LOCAL_INTERACTIVE_TESTED：1440×900、1024×900、820×1180、430×932、390×844、320×740；另测 820×700 横屏圆角。六字段完整、图片加载、无横向溢出。
- VERIFIED：C3 七个尺寸的进入前／起点／中点／终点；C4 桌面和手机真实进度与样式变化；1024 页尾收束。证据见三份 QA 和 JSON。
- TESTED：脚本语法、Node VM 回归检查，覆盖 C3 较晚范围、C4 标准范围与 1024 自然终点、偏好切换清理、RAF 合并和停止。
- VERIFIED：1440 / 390 的 no-JS 页面由 HTTP CSP 真正禁用脚本，内容与媒体保持可读。
- SIMULATED_BROWSER_REDUCED_MOTION：测试响应交换 motion media query 的条件，使正常浏览器实际渲染静态分支；运行时切换另由 VM 检查。真实 OS 偏好切换 NOT_VERIFIED。
- REMOTE_STATIC_REVIEW：精选截图、源文件差异、QA 与三段约 7.2 秒真实浏览器滚动录像。录像为时间戳截图编码，没有生成中间画面。
- 静态原型无编译步骤；未把脚本语法检查称为网站 build 或整站回归。

概念媒体不是生产服务、真伪判断或工艺性能证据。HTML 保留“独立示例”和“信息可查不等同于真伪判定”；图片中烘焙的验证区域继续被固定遮罩覆盖。

## 交付与停止点

用户本次明确要求 delta only，故不生成新的 Full-Web，也不重新打包旧媒体。Delta 以 A8-C Full-Web 为技术文件基底，不能单独作为完整网站；应用方式与下载入口见 REVIEW-MANIFEST。

仅五个运行文件和本次五份文档有变动；具体清单及 SHA256 在 manifest / source-checks 中。没有 commit、push。分支 main，HEAD `80b4a79e5d96163185081e193b37c06ce8bfa550`；最终 fresh Ahead 0 / Behind 0，工作区保留既有及本次未提交改动。等待本补丁评审，停止在 C4。
